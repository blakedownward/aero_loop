# AeroLoop Annotator

### Description:
this script loads audio samples into a gui so they can be verified and annotated. entirely non-destructive, siply appends annotation and marker files to folders in the data/raw directory